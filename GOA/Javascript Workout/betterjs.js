// WORKOUT
let variable1 = 3;
// 3 saved in variable
console.log(2);
// outputs 2 in console
// displays whatevers inside
console.log(variable1);
// displays saved variable written before
let calculation = 2 + 2;
// calculates 2+2 and then save it
console.log(calculation);
// displays saved variable, in console
console.log(calculation + 2);
// using a variable and adding 2, same as 4 + 2 (6)
// Js will substitude whatever is in the code
let result = calculation + 2;
// makes console.log(calculation + 2); shorter
console.log(result);

let message = 'hello';
// string defined, displays a text/str

// Syntax rules for variables
// we can name mostly anything a variable,
// cant name a variable let - special code, but can let1 and let2
// shouldn't start with number, javascript defines it as a number
// can't use any symbols except $ and _
// ; semicolon means the end of a code

console.log(message); console.log(';');
// will output texts but on different lines
// has a feature called Semicolon Insertion

// how to change a variable? for example that we used
variable1 = 5;
// at first displayed 3 remains and then change to 5
// Re-assigning Variable
// dont use let whn re-assigning a variable
// because this variable already exists and can define a new one
// with the same name
// last code before the project:
variable1 = variable1 + 1
// re-assigning a variable with itself
// if we console.log this code it will display 6
// variable 1 defined as 5 before changes the place (3)